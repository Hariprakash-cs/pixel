// ============================================
// USER AUTHENTICATION APIs
// ============================================

export const userAPI = {
  // Check if user is authenticated
  isAuthenticated: () => {
    return true; // Always return true since we're using Contentstack CMS
  },
};

export default userAPI;
